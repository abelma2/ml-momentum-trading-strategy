# Normalize line endings: store LF in the repo, check out native on each OS.
* text=auto eol=lf
# Windows PowerShell scripts use CRLF.
*.ps1 text eol=crlf
# Treat these as binary (no EOL/diff munging).
*.png binary
*.pdf binary
*.xlsx binary
*.csv binary
